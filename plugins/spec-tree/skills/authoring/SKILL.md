---
name: authoring
description: |
  When the user asks to add, define, create, or specify something — a feature, capability, decision, or spec.
  When the user says "let's add", "we need", "define", "plan", "spec out", "write up", invoke this.
allowed-tools: Read, Glob, Grep, Write, Edit
---

<!-- PLACEHOLDER: Full implementation in Phase 3 -->

<objective>

Author Spec Tree artifacts using templates from the `understanding` foundation skill. Supports creating product specs, decision records (ADR/PDR), enabler nodes, and outcome nodes. Includes project CLAUDE.md template for bootstrapping new Spec Tree projects.

</objective>
